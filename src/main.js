import {h, render} from 'preact';
import App from './App';
// import 'firebase/app';
// import 'firebase/auth';

// const config = {/* feed me */};
// firebase.initializeApp(config);

render(h(App), document.getElementById('root'));